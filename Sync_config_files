for f in /etc/portage/{make.conf,package.mask,package.unmask,package.use,package.accept_keywords}
do
  grep -Ev '^#|^$' "$f" | sort > ~/Funtoo/$(basename "$f")
done
cat /var/lib/portage/world > ~/Funtoo/World
cat /usr/src/linux/.config > ~/Funtoo/config-`uname -r`
